const contextList = {
  home: 'home',
  error: 'error',
};

const contextToPageNameMap = {
  home: 'Home Page',
  error: 'Error Page',
};

const context = {
  contextList,
  contextToPageNameMap,
};

module.exports = context;
